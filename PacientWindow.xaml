<Window x:Class="PolZhelnov.PacientWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Пациенты" Height="350" Width="400" WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Список пациентов" FontWeight="Bold" FontSize="14" Margin="0,0,0,10"/>

        <ListBox x:Name="PacientList" 
                 Grid.Row="1" 
                 Height="100" 
                 SelectionChanged="PacientList_SelectionChanged">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding FIOPacient}" />
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <Grid Grid.Row="2" Margin="0,10,0,0">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="ФИО:" VerticalAlignment="Center"/>
            <TextBox x:Name="TxtFIO" Grid.Column="1" Margin="5,0,0,0"/>

            <TextBlock Text="СНИЛС:" Grid.Row="1" VerticalAlignment="Center"/>
            <TextBox x:Name="TxtSnils" Grid.Row="1" Grid.Column="1" Margin="5,0,0,0"/>

            <TextBlock Text="Дата рождения:" Grid.Row="2" VerticalAlignment="Center"/>
            <TextBox x:Name="TxtBirth" Grid.Row="2" Grid.Column="1" Margin="5,0,0,0" 
                     Text="{Binding ElementName=TxtBirth, Path=Text}" 
                     TabIndex="2"/>

            <TextBlock Text="Телефон:" Grid.Row="3" VerticalAlignment="Center"/>
            <TextBox x:Name="TxtPhone" Grid.Row="3" Grid.Column="1" Margin="5,0,0,0"/>
        </Grid>

        <StackPanel Orientation="Horizontal" Grid.Row="3" Margin="0,10,0,0">
            <Button Content="Сохранить" Click="Save_Click" Width="80" Margin="0,0,5,0"/>
            <Button Content="Удалить" Click="Delete_Click" Width="80" Margin="0,0,5,0"/>
            <Button Content="Очистить" Click="Clear_Click" Width="80"/>
        </StackPanel>
    </Grid>
</Window>