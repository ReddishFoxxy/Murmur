<Window x:Class="PolZhelnov.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Поликлиника — Записи" Height="800" Width="1000"
        WindowStartupLocation="CenterScreen"
        Background="#F8F9FA">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Поликлиника — Управление"
                   FontSize="24"
                   FontWeight="Bold"
                   Foreground="#2C3E50"
                   Margin="0,0,0,20"
                   HorizontalAlignment="Center"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>

                <ColumnDefinition Width="300"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>


            <Border Grid.Column="0" 
                    Background="White" 
                    CornerRadius="8" 
                    Padding="15"
                    Margin="0,0,10,0"
                    BorderBrush="#CED4DA" 
                    BorderThickness="1">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Text="Список записей"
                               FontWeight="SemiBold"
                               FontSize="16"
                               Foreground="#495057"
                               Margin="0,0,0,10"/>

                    <TextBox x:Name="SearchBox"
                             Grid.Row="1"
                             Margin="0,0,0,10"
                             Padding="6,4"
                             FontSize="14"
                             BorderBrush="#DEE2E6"
                             Background="White"
                             TextChanged="SearchBox_TextChanged"
                             VerticalContentAlignment="Center"/>

                    <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,15">
                        <Button Content="А → Я"
                                Click="SortDocAsc_Click"
                                Background="#E9ECEF"
                                Foreground="#212529"
                                Padding="8,4"
                                FontSize="12"
                                BorderThickness="0"
                                Cursor="Hand"
                                Margin="0,0,6,0"/>
                        <Button Content="Я → А"
                                Click="SortDocDesc_Click"
                                Background="#E9ECEF"
                                Foreground="#212529"
                                Padding="8,4"
                                FontSize="12"
                                BorderThickness="0"
                                Cursor="Hand"/>
                    </StackPanel>

                    <ListBox x:Name="ZapisiList"
                             Grid.Row="3"
                             SelectionChanged="ZapisiList_SelectionChanged"
                             Background="Transparent"
                             BorderThickness="0"
                             FontSize="14"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Border Padding="10,8" 
                                        Background="White" 
                                        Margin="0,0,0,6" 
                                        CornerRadius="4"
                                        BorderBrush="#E9ECEF" 
                                        BorderThickness="1">
                                    <StackPanel>
                                        <TextBlock Text="{Binding Pacient.FIOPacient}" FontWeight="Bold" Foreground="#212529"/>
                                        <TextBlock Text="{Binding Doc.FIODoc}" FontStyle="Italic" Foreground="#495057"/>
                                        <TextBlock Text="{Binding datetimeZapis, StringFormat='dd.MM.yyyy HH:mm'}" Foreground="#6C757D"/>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </Border>

            <Border Grid.Column="1" 
                    Background="White" 
                    CornerRadius="8" 
                    Padding="25"
                    Margin="10,0,0,0"
                    BorderBrush="#CED4DA" 
                    BorderThickness="1">
                <StackPanel>
                    <TextBlock Text="Детали записи"
                               FontWeight="SemiBold"
                               FontSize="18"
                               Foreground="#495057"
                               Margin="0,0,0,20"/>

                    <TextBlock Text="Дата и время" Foreground="#495057" Margin="0,0,0,4"/>
                    <TextBox x:Name="DateTimeZap"
                             Padding="8,6"
                             FontSize="14"
                             Margin="0,0,0,18"
                             BorderBrush="#DEE2E6"
                             Background="White"/>

                    <TextBlock Text="Пациент (ФИО)" Foreground="#495057" Margin="0,0,0,4"/>
                    <TextBox x:Name="TextPac"
                             Padding="8,6"
                             FontSize="14"
                             Margin="0,0,0,18"
                             BorderBrush="#DEE2E6"
                             Background="White"/>

                    <TextBlock Text="Врач" Foreground="#495057" Margin="0,0,0,4"/>
                    <ComboBox x:Name="comboDoci"
                              Padding="6,4"
                              FontSize="14"
                              Margin="0,0,0,24"
                              BorderBrush="#DEE2E6"
                              Background="White"
                              DisplayMemberPath="FIODoc"/>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button x:Name="Save" 
                                Content="Сохранить" 
                                Click="Save_Click"
                                Width="110"
                                Height="36"
                                Margin="0,0,12,0"
                                Background="#28A745"
                                Foreground="White"
                                FontSize="14"
                                FontWeight="SemiBold"
                                BorderThickness="0"
                                Cursor="Hand">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="{TemplateBinding Background}" 
                                                        CornerRadius="5"
                                                        Padding="12,6">
                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#218838"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                        <Button x:Name="Back" 
                            Content="Вернуться" 
                            Click="Back_Click"
                            Width="110"
                            Height="36"
                            Margin="0,0,12,0"
                            Background="#28A745"
                            Foreground="White"
                            FontSize="12"
                            FontWeight="SemiBold"
                            BorderThickness="0"
                            Cursor="Hand">  
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="{TemplateBinding Background}" 
                                                    CornerRadius="5"
                                                    Padding="12,6">
                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#218838"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>

                        <Button x:Name="Clear" 
                                Content="Очистить" 
                                Click="Clear_Click"
                                Width="110"
                                Height="36"
                                Margin="0,0,12,0"
                                Background="#FFC107"
                                Foreground="Black"
                                FontSize="14"
                                FontWeight="SemiBold"
                                BorderThickness="0"
                                Cursor="Hand">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="{TemplateBinding Background}" 
                                                        CornerRadius="5"
                                                        Padding="12,6">
                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#E0A800"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>

                        <Button x:Name="Delete" 
                                Content="Удалить" 
                                Click="Delete_Click"
                                Width="110"
                                Height="36"
                                Background="#DC3545"
                                Foreground="White"
                                FontSize="14"
                                FontWeight="SemiBold"
                                BorderThickness="0"
                                Cursor="Hand">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="{TemplateBinding Background}" 
                                                        CornerRadius="5"
                                                        Padding="12,6">
                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#C82333"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,25,0,0">
            <Button Name="Doctori"
                    Content="Таблица врачей"
                    Click="Vrachi"
                    Padding="14,8"
                    Margin="0,0,25,0"
                    Background="#6C757D"
                    Foreground="White"
                    BorderThickness="0"
                    Cursor="Hand"
                    FontSize="14">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" CornerRadius="6">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#5A6268"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>

            <Button Name="Pacienti"
                    Content="Таблица пациентов"
                    Click="Pacienti_Click"
                    Padding="14,8"
                    Background="#007BFF"
                    Foreground="White"
                    BorderThickness="0"
                    Cursor="Hand"
                    FontSize="14">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" CornerRadius="6">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#0069D9"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </StackPanel>
    </Grid>
</Window>